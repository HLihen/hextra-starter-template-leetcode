# Hugo configuration file
title: 力扣算法题收集

# import hextra as module
module:
  imports:
    - path: github.com/imfing/hextra

markup:
  # allow raw html
  goldmark:
    renderer:
      unsafe: true

  # enable hextra syntax highlight
  highlight:
    noClasses: false

menu:
  main:
    - name: 算法
      pageRef: /docs
      weight: 1
    - name: Java
      pageRef: /java
      weight: 2
    - name: 搜索
      weight: 3
      params:
        type: search


params:
  navbar:
    displayTitle: true
    displayLogo: false

  footer:
    displayCopyright: false
    displayPoweredBy: true

  editURL:
    enable: true
    base: "https://github.com/HLihen/hextra-starter-template-leetcode/edit/main/content"
